spring:
  config:
    import: optional:classpath:application-prod.yml

app:
  security:
    require-production-secrets: true
    allow-api-key-auth: false
    jwt:
      allow-hmac-in-production: true
  tenancy:
    allow-header-tenant-selection: false
    require-tenant-context: true
